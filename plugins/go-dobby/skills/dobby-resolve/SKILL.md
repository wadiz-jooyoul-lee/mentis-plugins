---
name: dobby-resolve
description: 이슈/작업이 (지금으로선) 다 됐다고 판단될 때, 상태만 "해결"로 표시하는 비파괴 스킬. status.md의 현재 단계를 해결로 바꾸고 ## 해결 섹션에 근거(리뷰 클린·최신 test-runs 결과·통합 브랜치·일시)를 기록한다. 워크트리·메타 폴더를 삭제하지 않고 Jira 상태도 건드리지 않는다(메타만 갱신). 추가 수정 여지를 남기기 위한 단계로, 필요하면 폴더가 살아 있으니 dobby-impl/dobby-order로 이어서 작업할 수 있다. 실제 워크트리 정리는 사용자가 dobby-end로 직접 처리한다. 사용법 /dobby-resolve {키}.
---

# dobby-resolve

이슈/작업을 **"해결"로 표시**하는 비파괴 스킬. 상태만 바꾸고 **아무것도 삭제하지 않는다.** "테스트 통과"와 "해결로 선언"을 분리해, 통과했더라도 추가 수정 여지를 남긴다.

> 워크트리 실제 제거(정리)는 이 스킬이 아니라 **`dobby-end`**가 하며, 사용자가 직접 실행한다. `dobby-resolve`는 상태 표시까지만이다.

## 설정 (첫 실행 시 확인)

작업을 시작하기 전에 **`${CLAUDE_PLUGIN_ROOT}/reference/config.md`의 "설정 절차"를 그대로 따른다**: `~/.config/work-dobby/config.env`를 source 해 환경 변수를 불러오고, **이미 값이 있는 변수는 묻지 않고** 빠진 값만 규칙대로 채워 저장·export 한다. 메타 루트 `DOBBY_META`, 변수 목록·기본값, 폴더 배치(워크트리 `$DOBBY_WORKSPACE/subtree/` · 메타 `$DOBBY_META/`)는 모두 그 문서에 있다. 이하 메타 경로는 `$DOBBY_META` 기준.

## 입력

- `args`: `{키}`(이슈 키 또는 작업 키 `TASK-...`). 없으면 현재 브랜치에서 키를 추출하거나 물어본다.

## 사전 조건

- `$DOBBY_META/{키}/status.md`가 있어야 한다. 없으면 "먼저 dobby-order로 진행하라"고 안내하고 중단한다.

## 절차

### 1. 상태·근거 확인 (사실 기반)
- `status.md`의 현재 단계와 `단계별 진행`·`테스트 실행 이력`을 읽는다.
- **해결 근거를 확인한다**:
  - 최신 `$DOBBY_META/{키}/test-runs/{시각}/result.md`(있으면)의 집계 — **전부 PASS 권장**. 실패/중단이 있으면 그대로 해결로 올리지 말고 사용자에게 확인한다.
  - 리뷰 클린 여부(implementation.md/reviews) 및 통합 브랜치(K≥2면 상위 브랜치 통합 완료).
- 테스트 결과가 없어도 **사용자가 명시적으로 해결로 표시하라고 하면** 그 사실을 근거로 기록하고 진행한다(근거에 "사용자 지정"으로 남김).

### 2. 상태 표시 (비파괴)
- `status.md`의 **현재 단계 → `해결`**, `담당 스킬 → dobby-resolve`, 갱신 일시 기록.
- `## 해결` 섹션에 근거를 남긴다:
  ```markdown
  ## 해결
  - **처리 일시**: {YYYY-MM-DD HH:MM}
  - **근거**: 리뷰 클린({round}) · 테스트 결과 `test-runs/{시각}/`(집계 성공X/실패0/skip Z) · 통합 브랜치 {feature/루트키 또는 자기 브랜치}
  - **비고**: {추가 수정 여지 등}
  ```
- **삭제·제거는 하지 않는다**: 워크트리(`$DOBBY_WORKSPACE/subtree/{repo}-{키}`)와 메타 폴더(`$DOBBY_META/{키}/`)를 그대로 유지한다.
- **Jira 상태는 건드리지 않는다**(메타만 갱신). 실제 이슈 상태 전환이 필요하면 사용자가 직접 한다.

### 3. 안내
- 해결로 표시했음을 요약해 보여준다(근거 포함).
- **추가 수정이 필요하면** 폴더·워크트리가 살아 있으므로 `dobby-impl {키}`(직접) 또는 `dobby-order`(재오케스트레이션)로 이어서 작업할 수 있음을 안내한다.
- 워크트리를 실제로 정리하려면 **사용자가 `dobby-end`를 직접 실행**하면 됨을 안내한다.

## 주의

- **비파괴 원칙**: 이 스킬은 상태 표시만 한다. 워크트리·메타·브랜치·Jira를 삭제·변경하지 않는다.
- **사실 기반**: 해결 근거는 확인된 사실(테스트 결과·리뷰·통합)로만 기록한다. 실패가 있으면 임의로 해결로 올리지 않고 사용자에게 확인한다.
- 어려운 용어를 지양한다.
